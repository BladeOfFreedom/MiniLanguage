package Physics.PlayerClasses;

public enum PlayerOperations {
    //Basic opps
    MOVE, JUMP, TURN, DESCENT,

    //for statements that doesn't involve the player that should be skip (not being used now)
    SKIP
}
